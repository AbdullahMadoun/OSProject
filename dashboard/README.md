# CPU Scheduler Dashboard

Open `dashboard/index.html` directly in a browser. No server, npm install, or build step is required.

The dashboard uses the Tailwind CDN and vanilla JavaScript modules for scheduling, Gantt rendering, metrics, and comparison charts.
